# Milestone v0.2 — Foundation & Developer Experience

**Branch:** `milestone/v0.2-foundation`  
**Status:** 📋 Planned  
**Depends on:** v0.1 (MVP) ✅

---

## Goal

Make the project easy to contribute to, enforce code quality automatically, and ship a CI pipeline so every commit is verified.

---

## Task Breakdown

### 📦 Packaging

#### Add `pyproject.toml`

- Replace `requirements.txt` / `requirements-dev.txt` as the single source of truth for project metadata and dependencies
- Sections needed: `[project]`, `[project.optional-dependencies]` (`dev`), `[project.scripts]`, `[tool.ruff]`, `[tool.mypy]`, `[tool.pytest.ini_options]`, `[tool.coverage.run]`
- Keep `requirements.txt` as a generated pin file (`pip-compile pyproject.toml > requirements.txt`) for reproducible installs

```toml
[project.scripts]
rpgwatcher = "src.bot:main"
```

- Add a `main()` function to `bot.py` that calls `bot.run(token)` so the entry point works

#### Add `py.typed` marker

- Create `src/py.typed` (empty file) to declare the package as typed (PEP 561)

---

### 🔍 Code Quality

#### Add `ruff`

Configuration in `pyproject.toml`:

```toml
[tool.ruff]
line-length = 99
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "ANN", "B", "PLC", "PLE", "PLW"]
ignore = ["ANN101", "ANN102"]
```

#### Add `mypy`

Configuration in `pyproject.toml`:

```toml
[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true
```

Fix all strict-mode warnings in `src/`:
- Add return type annotations to all functions
- Annotate `_active_recordings` properly
- Type-narrow `interaction.user.voice` before use in `watch()`

#### Add `pre-commit`

`.pre-commit-config.yaml`:

```yaml
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.0
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.9.0
    hooks:
      - id: mypy
        additional_dependencies: [types-all]
```

---

### 🧪 Testing

#### Add tests for `bot.py`

New file: `tests/test_bot.py`

Tests to write:

| Test | What it covers |
|------|---------------|
| `test_on_ready_syncs_commands` | `on_ready` calls `bot.tree.sync()` and logs the count |
| `test_watch_no_voice_state` | Returns an ephemeral error if the user is not in a voice channel |
| `test_watch_already_recording` | Returns a "already recording" message if guild has an active session |
| `test_watch_joins_and_starts_sink` | Happy path: joins VC, starts `RecordingSink`, stores in `_active_recordings` |
| `test_unwatch_no_active_recording` | Returns an error if no recording is in progress for this guild |
| `test_unwatch_stops_recording` | Calls `vc.stop_recording()` and kicks off the pipeline callback |
| `test_pipeline_callback_full_success` | All stages succeed; final embed posted and VC disconnected |
| `test_pipeline_callback_transcription_failure` | Transcription raises; error message posted, VC disconnected |
| `test_pipeline_callback_summarisation_failure` | Summarisation raises; error posted, VC disconnected |
| `test_pipeline_callback_obsidian_failure` | Obsidian save raises; summary still posted as fallback |

Use `pytest-asyncio` and `unittest.mock.AsyncMock` for all async tests.

#### Integration smoke test

New file: `tests/test_integration.py`

- Mock the Discord layer entirely
- Run `_on_recording_finished` with a real `RecordingSink` fixture and mocked `transcribe`/`summarize`/`save_to_obsidian`
- Assert the completion message is posted and the VC is disconnected

#### Coverage enforcement

```toml
[tool.coverage.run]
source = ["src"]
omit = ["src/__init__.py"]

[tool.coverage.report]
fail_under = 80
```

Add `pytest --cov=src --cov-fail-under=80` to CI.

---

### 🤖 CI

#### `.github/workflows/ci.yml`

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install -e ".[dev]"
      - run: ruff check src tests
      - run: ruff format --check src tests
      - run: mypy src
      - run: pytest --cov=src --cov-fail-under=80
```

---

### 📝 Project Hygiene

#### `.env.example`

Full template with all supported env vars, documented inline — see [configuration.md](../configuration.md) for the full content.

#### `CHANGELOG.md`

Initial file following [Keep a Changelog](https://keepachangelog.com/):

```markdown
# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

## [0.1.0] - 2026-MM-DD
### Added
- Initial MVP release
...
```

#### `CODE_OF_CONDUCT.md`

Contributor Covenant v2.1 — standard boilerplate with maintainer contact email.

---

## Acceptance Criteria

- [ ] `pip install -e ".[dev]"` installs all dependencies from `pyproject.toml`
- [ ] `rpgwatcher` entry point exists and calls `bot.run()`
- [ ] `ruff check src tests` exits 0
- [ ] `mypy src` exits 0 with no errors
- [ ] `pytest --cov=src --cov-fail-under=80` passes
- [ ] CI workflow green on `ubuntu-latest` for Python 3.11 and 3.12
- [ ] `.env.example` present and covers all env vars
- [ ] `CHANGELOG.md` present with a v0.1.0 entry
