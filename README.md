# TheWatcher
An open-source Discord bot that records a voice channel, saves the audio locally, passes it through a small, local transcription model (Whisper.cpp), and uses a local LLM (Ollama) to generate a Markdown summary that is pushed directly into the GM's Obsidian vault.
